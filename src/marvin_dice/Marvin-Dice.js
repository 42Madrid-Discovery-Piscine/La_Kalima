let numeroRandom = ""

let numeroUsuario = ""

let boolean = false
 function simon(){
 console.log(numeroRandom)

    do {
        numeroRandom = ""
        let i = 0
        for (let index = 0; index < 9; index++) {
            numeroRandom += ("" + Math.floor(Math.random()* (9) + 1)) }
        console.log(numeroRandom)
        const myTimeout = setTimeout(myGreeting, 5000);

        document.getElementById ()
      
      
        do{

        numeroUsuario += ""+

        i++

        }while (1==9)

        if (numeroUsuario == numeroRandom) { alert("entra aqui")

        boolean = true

        } else {

        alert("Mal, Prueba otra vez ")
        }
    }while (boolean != true);
     window.location.hred = "https://www.google.es"
 }